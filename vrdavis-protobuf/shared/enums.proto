syntax = "proto3";
package VRDAVis;

enum EventType {
    EMPTY_EVENT = 0;
    REGISTER_VIEWER = 1;
    REGISTER_VIEWER_ACK = 2;
    CUBE_DATA = 3;
}

enum SessionType {
    NEW = 0;
    RESUMED = 1;
}